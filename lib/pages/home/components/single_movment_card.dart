import 'package:flutter/material.dart';

class SingleMovementCard extends StatelessWidget {
  const SingleMovementCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
        child: Container(
      width: double.infinity,
      height: 50,
    ));
  }
}
